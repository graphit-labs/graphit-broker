database:
  driver: sqlite
  dsn: /var/lib/graphit-broker/graphit-broker-smoke.db
  max_open_conns: 1
  max_idle_conns: 1
  conn_max_lifetime: 3m

server:
  address: ":8080"

services:
  embeddings:
    enabled: false
  rerank:
    enabled: false
  s3:
    enabled: false
